services:
  backend:
    image: ghcr.io/mvelusce/pi-habits-tracker-backend:latest
    container_name: habits-tracker-backend
    ports:
      - "${BACKEND_PORT:-8000}:8000"
    volumes:
      - ./data:/app/data
    environment:
      - DATABASE_URL=sqlite:///./data/habits_tracker.db
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/health"]
      interval: 30s
      timeout: 10s
      retries: 3

  frontend:
    image: ghcr.io/mvelusce/pi-habits-tracker-frontend:latest
    container_name: habits-tracker-frontend
    ports:
      - "${FRONTEND_PORT:-3000}:80"
    environment:
      # Pass backend port from .env so frontend can auto-configure API URL
      - BACKEND_PORT=${BACKEND_PORT:-8000}
      # Optional: Override API URL completely (for separate subdomains)
      - VITE_API_URL=${VITE_API_URL:-}
    depends_on:
      - backend
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "wget", "--quiet", "--tries=1", "--spider", "http://localhost:80"]
      interval: 30s
      timeout: 10s
      retries: 3

volumes:
  data:
    driver: local

